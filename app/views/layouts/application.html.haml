!!!
%html
  %head
    %title TreeNavExperiment
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          = link_to "Tree Navigation", root_path, class: :brand
          %ul.nav
            - Page.roots.each do |page|
              %li{ class: (:active if @page && @page.root == page) }= link_to page.name, page

    .container
      = yield
